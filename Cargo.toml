[package]
name = "whatisit"
version = "0.3.0"
edition = "2024"
description = "Turn plain English into shell commands with a local llama.cpp model"
license = "Apache-2.0"

[[bin]]
name = "whatisit"
path = "src/main.rs"

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
